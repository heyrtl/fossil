{
  "name": "@openfossil/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute fossil --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute fossil --remote --file=./schema.sql"
  },
  "dependencies": {
    "@openfossil/core": "^0.1.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.65.0"
  }
}